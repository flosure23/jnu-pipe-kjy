This file is a merged representation of the entire codebase, combined into a single document by Repomix.

<file_summary>
This section contains a summary of this file.

<purpose>
This file contains a packed representation of the entire repository's contents.
It is designed to be easily consumable by AI systems for analysis, code review,
or other automated processes.
</purpose>

<file_format>
The content is organized as follows:
1. This summary section
2. Repository information
3. Directory structure
4. Repository files (if enabled)
5. Multiple file entries, each consisting of:
  - File path as an attribute
  - Full contents of the file
</file_format>

<usage_guidelines>
- This file should be treated as read-only. Any changes should be made to the
  original repository files, not this packed version.
- When processing this file, use the file path to distinguish
  between different files in the repository.
- Be aware that this file may contain sensitive information. Handle it with
  the same level of security as you would the original repository.
</usage_guidelines>

<notes>
- Some files may have been excluded based on .gitignore rules and Repomix's configuration
- Binary files are not included in this packed representation. Please refer to the Repository Structure section for a complete list of file paths, including binary files
- Files matching patterns in .gitignore are excluded
- Files matching default ignore patterns are excluded
- Files are sorted by Git change count (files with more changes are at the bottom)
</notes>

</file_summary>

<directory_structure>
.github/workflows/ci.yml
.gitignore
bank_mock/account.py
bank_mock/logger_stub.py
bank_mock/test_account_mock.py
bank_mock/test_account_stub.py
bank_project/.coverage
bank_project/src/account.py
bank_project/tests/test_mock.py
docker_sample/app.py
docker_sample/Dockerfile
mock_practice/price_service.py
mock_practice/test_price_mock_as_stub.py
mock_practice/test_price_stub.py
SpamCheck/app/main.py
SpamCheck/app/spam.py
SpamCheck/requirements.txt
SpamCheck/static/index.html
SpamCheck/tests/conftest.py
SpamCheck/tests/test_api.py
SpamCheck/tests/test_spam.py
test_practice/calculator.py
test_practice/calculator2.py
test_practice/coverage_practice/.coverage
test_practice/coverage_practice/calculator.py
test_practice/coverage_practice/test_calculator.py
test_practice/coverage_practice/test_calculator2.py
test_practice/run_all_tests.py
test_practice/run_selected_tests.py
test_practice/test_add.py
test_practice/test_calculator.py
test_practice/test_calculator2.py
test_practice/test_name_rule.py
test_practice/test_subtract.py
</directory_structure>

<files>
This section contains the contents of the repository's files.

<file path="docker_sample/app.py">
from flask import Flask

app = Flask(__name__)

@app.route("/")
def hello():
    return "Hello, Docker! 🚀"

if __name__ == "__main__":
    app.run(host="0.0.0.0", port=5000)
</file>

<file path="docker_sample/Dockerfile">
FROM python:3.9-slim

WORKDIR /app

COPY app.py .

RUN pip install flask

CMD ["python", "app.py"]
</file>

<file path=".github/workflows/ci.yml">
name: CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./SpamCheck

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run tests
        run: pytest
</file>

<file path="bank_mock/account.py">
class InsufficientFundsError(Exception):
    pass


class Account:
    def __init__(self, logger):
        self.balance = 0
        self.logger = logger

    def deposit(self, amount):
        self.balance += amount
        self.logger.log(f"Deposited {amount}")
        return self.balance

    def withdraw(self, amount):
        if self.balance < amount:
            self.logger.log("Withdrawal failed: insufficient funds")
            raise InsufficientFundsError("잔액 부족")

        self.balance -= amount
        self.logger.log(f"Withdrew {amount}")
        return self.balance
</file>

<file path="bank_mock/logger_stub.py">
class StubLogger:
    def log(self, message):
        return "logged"
</file>

<file path="bank_mock/test_account_mock.py">
import unittest
from unittest.mock import Mock
from account import Account, InsufficientFundsError


class TestAccountWithMock(unittest.TestCase):
    def setUp(self):
        self.mock_logger = Mock()
        self.account = Account(self.mock_logger)

    def test_deposit_calls_logger(self):
        self.account.deposit(200)
        self.mock_logger.log.assert_called_with("Deposited 200")

    def test_withdraw_calls_logger(self):
        self.account.deposit(100)
        self.account.withdraw(50)
        self.mock_logger.log.assert_any_call("Withdrew 50")

    def test_withdraw_insufficient_funds_logs_and_raises(self):
        with self.assertRaises(InsufficientFundsError):
            self.account.withdraw(30)

        self.mock_logger.log.assert_called_with("Withdrawal failed: insufficient funds")


if __name__ == "__main__":
    unittest.main(verbosity=2)
</file>

<file path="bank_mock/test_account_stub.py">
import unittest
from account import Account, InsufficientFundsError
from logger_stub import StubLogger


class TestAccountWithStub(unittest.TestCase):
    def setUp(self):
        self.logger = StubLogger()
        self.account = Account(self.logger)

    def test_deposit_increases_balance(self):
        self.account.deposit(100)
        self.assertEqual(self.account.balance, 100)

    def test_withdraw_raises_error_on_insufficient_funds(self):
        with self.assertRaises(InsufficientFundsError):
            self.account.withdraw(50)


if __name__ == "__main__":
    unittest.main(verbosity=2)
</file>

<file path="bank_project/src/account.py">
class InsufficientFundsError(Exception):
    pass


class Account:
    def __init__(self, logger):
        self.balance = 0
        self.logger = logger

    def deposit(self, amount):
        self.balance += amount
        self.logger.log(f"Deposited {amount}")
        return self.balance

    def withdraw(self, amount):
        if self.balance < amount:
            self.logger.log("Withdrawal failed: insufficient funds")
            raise InsufficientFundsError("잔액 부족")

        self.balance -= amount
        self.logger.log(f"Withdrew {amount}")
        return self.balance
</file>

<file path="bank_project/tests/test_mock.py">
import unittest
from unittest.mock import Mock
import os
import sys

sys.path.append(os.path.abspath(os.path.join(os.path.dirname(__file__), "..", "src")))

from account import Account, InsufficientFundsError


class TestAccountWithMock(unittest.TestCase):
    def setUp(self):
        self.mock_logger = Mock()
        self.account = Account(self.mock_logger)

    def test_deposit_calls_logger(self):
        new_balance = self.account.deposit(200)

        self.assertEqual(new_balance, 200)
        self.assertEqual(self.account.balance, 200)
        self.mock_logger.log.assert_called_with("Deposited 200")

    def test_withdraw_calls_logger(self):
        self.account.deposit(100)
        new_balance = self.account.withdraw(50)

        self.assertEqual(new_balance, 50)
        self.assertEqual(self.account.balance, 50)
        self.mock_logger.log.assert_any_call("Withdrew 50")

    def test_withdraw_insufficient_funds_logs_and_raises(self):
        with self.assertRaises(InsufficientFundsError):
            self.account.withdraw(30)

        self.mock_logger.log.assert_called_with("Withdrawal failed: insufficient funds")


if __name__ == "__main__":
    unittest.main(verbosity=2)
</file>

<file path="mock_practice/price_service.py">
def get_discount_func(user_id):
    # 원래는 외부 API를 호출한다고 가정
    # 테스트에서는 이 함수를 직접 쓰지 않을 수도 있다
    raise NotImplementedError("실제 할인 API는 아직 연결하지 않음")


def calculate_price(user_id, price, get_discount_func):
    return price - get_discount_func(user_id)
</file>

<file path="mock_practice/test_price_mock_as_stub.py">
import unittest
from unittest.mock import Mock
from price_service import calculate_price


class TestCalculatePriceWithMock(unittest.TestCase):
    def test_discount_applied_with_mock_as_stub(self):
        mock_func = Mock()
        mock_func.return_value = 2000

        result = calculate_price("user123", 8000, mock_func)

        self.assertEqual(result, 6000)


if __name__ == "__main__":
    unittest.main()
</file>

<file path="mock_practice/test_price_stub.py">
import unittest
from price_service import calculate_price


def stub_get_discount(user_id):
    return 1000


class TestCalculatePriceWithStub(unittest.TestCase):
    def test_discount_applied(self):
        result = calculate_price("user1", 5000, stub_get_discount)
        self.assertEqual(result, 4000)


if __name__ == "__main__":
    unittest.main()
</file>

<file path="SpamCheck/app/spam.py">
def check_spam(text: str) -> tuple[str, int]:
    # 소문자 변환 및 양끝 공백 제거
    text = text.lower().strip()
    
    if text == "":
        return "ham", 0
        
    spam_keywords = [
        "free", "win", "winner", "prize", "click",
        "buy now", "urgent", "cash", "money", "offer", "deal"
    ]
    
    hit = 0
    for kw in spam_keywords:
        # 디버깅을 위한 출력 (실제 서비스 시에는 주석 처리 권장)
        # print(kw, text) 
        if kw in text:
            hit += 1
            
    # 키워드가 2개 이상 검출되면 spam, 아니면 ham 반환
    label = "spam" if hit >= 2 else "ham"
    
    return label, hit
</file>

<file path="SpamCheck/tests/conftest.py">
import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parents[1]))
</file>

<file path="SpamCheck/tests/test_api.py">
from fastapi.testclient import TestClient
from app.main import app

client = TestClient(app)


def test_classify_api_contract_ham():
    response = client.post("/classify", json={"text": "hello"})
    assert response.status_code == 200

    data = response.json()
    assert "label" in data
    assert "score" in data
    assert data["label"] == "ham"
    assert data["score"] == 0


def test_classify_api_contract_spam():
    response = client.post("/classify", json={"text": "free click"})
    assert response.status_code == 200

    data = response.json()
    assert data["label"] == "spam"
    assert data["score"] >= 2


def test_classify_api_empty_text():
    response = client.post("/classify", json={"text": ""})
    assert response.status_code == 200

    data = response.json()
    assert data["label"] == "ham"
    assert data["score"] == 0


def test_classify_api_invalid_body():
    response = client.post("/classify", json={})
    assert response.status_code == 422
</file>

<file path="SpamCheck/tests/test_spam.py">
from app.spam import check_spam


def test_check_spam_empty():
    label, score = check_spam("")
    assert label == "ham"
    assert score == 0


def test_check_spam_ham():
    label, score = check_spam("hello nice to meet you")
    assert label == "ham"
    assert score == 0


def test_check_spam_spam():
    label, score = check_spam("free prize click now")
    assert label == "spam"
    assert score >= 2


def test_check_spam_case_insensitive():
    label, score = check_spam("FREE Prize")
    assert label == "spam"
    assert score == 2


def test_check_spam_strip_whitespace():
    label, score = check_spam("   free prize   ")
    assert label == "spam"
    assert score == 2
</file>

<file path="test_practice/calculator.py">
class Calculator:
    def add(self, a, b):
        return a + b
</file>

<file path="test_practice/calculator2.py">
class Calculator:
    def add(self, a, b):
        return a + b

    def subtract(self, a, b):
        return a - b
</file>

<file path="test_practice/coverage_practice/calculator.py">
def add(a, b):
    return a + b


def subtract(a, b):
    return a - b


def divide(a, b):
    if b == 0:
        return None
    return a / b
</file>

<file path="test_practice/coverage_practice/test_calculator.py">
import unittest
import calculator


class TestCalculator(unittest.TestCase):
    def test_add(self):
        self.assertEqual(calculator.add(2, 3), 5)

    def test_divide(self):
        self.assertEqual(calculator.divide(10, 2), 5)


if __name__ == "__main__":
    unittest.main()
</file>

<file path="test_practice/coverage_practice/test_calculator2.py">
import unittest
import calculator


class TestCalculator2(unittest.TestCase):
    def test_subtract(self):
        self.assertEqual(calculator.subtract(5, 2), 3)


if __name__ == "__main__":
    unittest.main()
</file>

<file path="test_practice/run_all_tests.py">
import unittest
from test_add import TestAdd
from test_subtract import TestSubtract


suite = unittest.TestSuite()

loader = unittest.TestLoader()
suite.addTest(loader.loadTestsFromTestCase(TestAdd))
suite.addTest(loader.loadTestsFromTestCase(TestSubtract))

runner = unittest.TextTestRunner()
runner.run(suite)
</file>

<file path="test_practice/run_selected_tests.py">
import unittest
from test_add import TestAdd
from test_subtract import TestSubtract


suite = unittest.TestSuite()

suite.addTest(TestAdd("test_add_positive"))
suite.addTest(TestSubtract("test_subtract_negative"))

runner = unittest.TextTestRunner()
runner.run(suite)
</file>

<file path="test_practice/test_add.py">
import unittest
from calculator import Calculator


class TestAdd(unittest.TestCase):
    def test_add_positive(self):
        self.assertEqual(Calculator().add(2, 3), 5)

    def test_add_zero(self):
        self.assertEqual(Calculator().add(0, 0), 0)


if __name__ == "__main__":
    unittest.main()
</file>

<file path="test_practice/test_calculator.py">
import unittest
from calculator import Calculator


class TestCalculator(unittest.TestCase):
    def test_add(self):
        calc = Calculator()
        self.assertEqual(calc.add(2, 3), 5)   # 일부러 틀리게 작성
        self.assertEqual(calc.add(-1, 1), 0)  # 일부러 틀리게 작성
        self.assertEqual(calc.add(0, 0), 0)


if __name__ == "__main__":
    unittest.main()
</file>

<file path="test_practice/test_calculator2.py">
import unittest
from calculator2 import Calculator


class TestCalculator(unittest.TestCase):
    def setUp(self):
        print(">> setUp 실행됨")
        self.calc = Calculator()

    def tearDown(self):
        print("<< tearDown 실행됨")

    def test_add(self):
        self.assertEqual(self.calc.add(2, 3), 5)

    def test_subtract(self):
        self.assertEqual(self.calc.subtract(5, 3), 2)


if __name__ == "__main__":
    unittest.main()
</file>

<file path="test_practice/test_name_rule.py">
import unittest


class MyTest(unittest.TestCase):
    def test_add(self):
        self.assertEqual(1 + 1, 2)

    def test_add2(self):
        self.assertEqual(1 + 1, 2)

    def calc_add(self):
        self.assertEqual(1 + 1, 2)


if __name__ == "__main__":
    unittest.main()
</file>

<file path="test_practice/test_subtract.py">
import unittest
from calculator import Calculator


class TestSubtract(unittest.TestCase):
    def test_subtract_positive(self):
        self.assertEqual(Calculator().subtract(5, 3), 2)

    def test_subtract_negative(self):
        self.assertEqual(Calculator().subtract(3, 5), -2)


if __name__ == "__main__":
    unittest.main()
</file>

<file path=".gitignore">
__pycache__/
*.pyc
.venv/
venv/
.env
.DS_Store
.idea/
.vscode/
.pytest_cache/
</file>

<file path="SpamCheck/app/main.py">
from fastapi import FastAPI, Request
from fastapi.responses import HTMLResponse
from fastapi.staticfiles import StaticFiles
from app.spam import check_spam
from pydantic import BaseModel

# FastAPI 기반 웹 앱 생성
# /docs (Swagger UI)에 표기되는 이름
app = FastAPI(title="SpamCheck Web")

# 정적 HTML 서빙: static 안에 파일들을 URL로 접근가능하게 설정
# {URL}/static/…… 으로 접근 가능
app.mount("/static", StaticFiles(directory="static"), name="static")

# 메인 페이지 (/) 처리: "/"로 접속 시 index.html 반환
@app.get("/", response_class=HTMLResponse)
def home():
    with open("static/index.html", encoding="utf-8") as f:
        return f.read()

class ClassifyRequest(BaseModel):
    text: str

# classify 요청이 올 때 처리할 로직
# async: 비동기 처리 (서버가 요청을 기다리는 동안 다른 요청도 처리 가능)
@app.post("/classify")
async def classify(payload: ClassifyRequest):
    text = payload.text 
    
    # spam.py의 check_spam 함수를 호출하여 결과 반환
    label, score = check_spam(text)
    
    return {
        "label": label, 
        "score": score
    }

# 실행은 터미널에서 uvicorn app.main:app --reload 명령어로 수행하거나
# 아래 주석을 해제하여 직접 실행할 수 있습니다.
# if __name__ == "__main__":
#     import uvicorn
#     uvicorn.run(app, host="127.0.0.1", port=8000)
</file>

<file path="SpamCheck/requirements.txt">
fastapi
uvicorn
python-multipart
pytest
httpx
</file>

<file path="SpamCheck/static/index.html">
<!doctype html>
<html>

<head>
    <meta charset="utf-8">
    <title>적당한 스팸 판별기 (AI Spam Checker)</title>
</head>

<body>
    <h1>스팸 체크기</h1>
    <textarea id="text" rows="5" cols="60" placeholder="스팸으로 의심되는 메시지를 붙여넣어주세요!"></textarea><br><br>
    <button onclick="classify()">분류</button>
    <h3 id="result"></h3>

    <script>
        function classify() {
            const text = document.getElementById("text").value;

            if (!text.trim()) {
                document.getElementById("result").innerText = "메시지를 입력해주세요.";
                return;
            }

            fetch("/classify", {
                method: "POST",
                headers: {
                    "Content-Type": "application/json"
                },
                body: JSON.stringify({ text: text })
            })
                .then(res => res.json())
                .then(data => {
                    document.getElementById("result").innerText =
                        "결과: " + data.label + " (score=" + data.score + ")";
                })
                .catch(() => {
                    document.getElementById("result").innerText = "에러 발생";
                });
        }
    </script>
</body>

</html>
</file>

</files>
